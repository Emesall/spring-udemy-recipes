spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rec_prod
    username: rec_prod_user
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false